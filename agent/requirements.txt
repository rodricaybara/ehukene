# Dependencias del agente EHUkene
# El core usa únicamente librería estándar de Python.
# Los plugins usan subprocess + PowerShell para consultas WMI,
# sin dependencia del paquete wmi (incompatible con Python 3.13+).

# Testing
pytest>=8.0.0

# Empaquetado
pyinstaller>=6.10.0
